name: CodeQL

on:
  workflow_call:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * 0'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Initialize the CodeQL tools
      uses: github/codeql-action/init@v2
      with:
        languages: javascript

    - name: Autobuild
      uses: github/codeql-action/autobuild@v2

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v2
      with:
        output: javascript.sarif
        category: 'javascript-analysis'  # Unique category for this analysis

    - name: List files for debugging
      run: ls -la

    - name: Upload SARIF File to GitHub
      uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: javascript.sarif
        category: 'javascript-analysis'  # Match the unique category specified above
